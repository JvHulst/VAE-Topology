"""Everything specific to the paper: data generation, figures, train and render scripts.

Nothing here is meant to be imported by a user of the library; ``topovae`` is.
"""

#: The seed every experiment runs under, so a rerun reproduces the figures exactly.
SEED = 42
